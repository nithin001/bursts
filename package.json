{
  "name": "bursts",
  "license": "MIT",
  "engines" : { "node": "11.10.1", "yarn": "1.13.0" },
  "scripts": {
    "build": "yarn --cwd bursts-react install && yarn --cwd bursts-react build && sh assets-generate.sh",
    "test": "yarn --cwd bursts-react test --no-watch"
  }
}
